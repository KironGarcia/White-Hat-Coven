/**
 * Tipografia pixel do WHC — Press Start 2P (estilo RPG clássico).
 * O nome é o da tabela da TTF. No Android a APK registra essa família
 * nativamente (plugin expo-font → ReactFontManager). Não usar useFonts:
 * isso chama ExpoAsset.downloadAsync e trava se AppDirectories não ligar.
 */

export const FONTE_PIXEL = 'Press Start 2P';
